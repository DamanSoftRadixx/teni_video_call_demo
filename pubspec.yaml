name: teni_video_call_demo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2



dependencies:
  flutter:
    sdk: flutter
  livekit_components:
    path: ./livekit_components
  cupertino_icons: ^1.0.8
  # state
  flutter_bloc: ^8.1.6
  fpdart: ^1.1.1
  get_it: ^7.6.4
  loader_overlay: ^5.0.0
  shimmer: ^3.0.0

  #code gernation realted
  equatable: ^2.0.7
  # freezed_annotation: ^3.1.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

  # localization
  flutter_localization: ^0.3.3
  intl: ^0.20.2
  # network
  dio: ^5.3.3
  logger: ^2.0.2
  internet_connection_checker_plus: ^2.7.2
  # routes management
  go_router: ^16.0.0
  # image loading
  cached_network_image: ^3.4.1
  flutter_svg: ^2.2.0
  # local storage
  flutter_secure_storage: ^9.2.4
  # isar: ^3.1.0+1
  # # isar_flutter_libs: ^3.1.0+1
  # isar_flutter_libs: 
  #   git:
  #     url: https://github.com/Deepjyoti120/isar_flutter_libs.git
  #     ref: main
  # flutter_cache_manager: ^3.4.1
  # core dependencies
  flutter_screenutil: ^5.9.3
  flutter_spinkit: ^5.2.1
  device_info_plus: ^12.2.0
  # dropdown_button2: ^2.3.9
  
  
dependency_overrides:
  device_info_plus: ^12.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
    # code gernation realted
  # freezed: ^3.1.0
  freezed: ^2.4.2
  # build_runner: ^2.5.4
  build_runner: ^2.4.6
  # json_serializable: ^6.9.5
  json_serializable: ^6.7.1
  analyzer: ^5.13.0


# need to add this inside analysis_options.yaml file
# analyzer:
#   exclude:
#     - "**/*.g.dart"
#     - "**/*.freezed.dart"
#   errors:
#     invalid_annotation_target: ignore

flutter:
  uses-material-design: true
  assets:
    - assets/svg/
    - assets/png/
